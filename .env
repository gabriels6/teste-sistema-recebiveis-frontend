# URL base da API de recebiveis.
# Deixe vazio para usar o proxy de desenvolvimento configurado no package.json
# (as chamadas relativas "/api/..." serao encaminhadas para http://localhost:8080).
REACT_APP_API_URL=
